export interface LoginFieldType {
    username?: string;
    password?: string;
    remember?: string;
}

export interface SignupFieldType {
    username?: string;
    password?: string;
    confirmPassword?: string;
}